<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <groupId>com.ctut.wms</groupId>
    <artifactId>wms-ecommerce-system</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <!-- 1. BẮT BUỘC: Khai báo đây là dự án cha, chỉ dùng để quản lý (đóng gói dạng pom) -->
    <packaging>pom</packaging>

    <name>wms-ecommerce-system</name>
    <description>Hệ thống WMS và E-commerce (Dự án cha)</description>

    <properties>
        <java.version>21</java.version>
        <spring-cloud.version>2023.0.3</spring-cloud.version>
    </properties>

    <!-- 2. LIỆT KÊ CÁC MODULE CON: IntelliJ và Maven sẽ dựa vào đây để gom nhóm -->
    <modules>
        <module>api-gateway</module>
        <module>discovery-server</module>
        <module>identity-service</module>
        <module>interaction-service</module>
        <module>order-service</module>
        <module>payment-service</module>
        <module>wms-core-service</module>
    </modules>

    <!-- 3. QUẢN LÝ PHIÊN BẢN CHUNG (Tùy chọn nhưng khuyên dùng) -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>